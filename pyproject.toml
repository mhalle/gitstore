[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gitstore"
version = "0.15.0"
requires-python = ">=3.10"
license = "Apache-2.0"
dependencies = [
    "pygit2>=1.14.0",
    "click>=8.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[project.scripts]
gitstore = "gitstore.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/gitstore"]

[tool.pytest.ini_options]
testpaths = ["tests"]
